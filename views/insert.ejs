<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <script src="https://cdn.ckeditor.com/4.19.0/standard/ckeditor.js"></script>

    <title>Insert or Update</title>
</head>
<body>
    <%- include('header'); -%>
    <main role="main" class="container">
        <section class="jumbotron text-center">
            <div class="container mt-4">
                <div class="card">
                    <div class="card-body">
            
                        <h2><%= (allData) ? "Update Blog" : 'Insert New Blog' %></h2>
                        <form class=" mt-4" action="/insert" method="POST">
                            <input type="hidden" name="_id" value="<%= (allData) ? allData._id : '' %>" >
                            <div class="form-group">
                                <input type="text" class="form-control col" placeholder="Enter title" name="title" value= "<%= (allData) ? allData.title : '' %>">
                            </div>
                            <div class="form-group">
                                <textarea  class="form-control col" name="body" id="bodytext" placeholder="Enter Content" rows="4" cols="50"> <%= (allData) ? allData.body : '' %></textarea>
                            </div>
                            <button type="submit" class="btn btn-primary"><%= (allData) ? "Update" : "Submit" %> </button>
                        </form>
                    </div>
                </div>
            </div>
        </section>
    </main>
    
    <%- include('footer'); -%>
    <script>
        CKEDITOR.replace( 'bodytext' );
    </script>
</body>
</html>
